# Project Configuration

This project was auto-initialized by Claude Code.

## Quick Start

```bash
# If you need Docker setup, run:
/docker-init

# Start development
npm run dev  # or your preferred command
```

## Docker Status

Run `/docker-init` to set up Docker containers for this project.

Once configured, the session hook will automatically:
- Check container health on startup
- Suggest recovery commands if containers are unhealthy
- Monitor running services

## Available Commands

- `/docker-init` - Interactive Docker setup wizard
